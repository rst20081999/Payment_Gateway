<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.payment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login_activity.login_signup_activity"/>
        <activity android:name=".pass_activity.EnterPassActivity" />
        <activity android:name=".pass_activity.CreatePassActivity" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".login_activity.regiter_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_activity.OtpActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>